<!DOCTYPE html>
<html>
<head>
   <title><%= content_for?(:title) ? yield(:title) : "Oracul" %></title>
   <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Predict a founding by a name" %>">
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<!-- 	<header class="navbar navbar-fixed-top">
		<nav class="navbar-inner">
			<div class="container">
				<%# render 'layouts/navigation' %>
			</div>
		</nav>
    </header> -->

	<div class="container">
		<div class="content">
			<div class="row">
				<div class="span12">
					<%= render 'layouts/messages' %>
					<%= yield %>
				</div>
			</div>

			<footer>
			</footer>
		</div>
	</div>

</body>
</html>
